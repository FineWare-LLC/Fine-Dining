MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_very_strong_and_secret_key_here
MONGO_ENCRYPTION_KEY=32_byte_hex_key
MONGO_ENCRYPTION_SIGNING_KEY=64_byte_hex_key
# Generate keys with:
#   openssl rand -hex 32  # encryption key
#   openssl rand -hex 64  # signing key
# Note: The code will automatically convert these hex keys to base64 format
GOOGLE_PLACES_API_KEY=
# Leave blank to always use the Overpass backup provider
# Optional custom Overpass endpoint used when Google Places fails
OVERPASS_URL=https://overpass-api.de/api/interpreter

# Places API Keys for Nearby Restaurants feature
# Primary provider: Geoapify (3,000 free credits/day)
GEOAPIFY_API_KEY=your_geoapify_api_key
# Fallback providers (configure at least one for redundancy)
YELP_API_KEY=your_yelp_api_key
FOURSQUARE_API_KEY=your_foursquare_api_key
OPENTRIPMAP_API_KEY=your_opentripmap_api_key
