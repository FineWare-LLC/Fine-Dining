# Branching Strategy

This project uses a simple Git workflow:

- `main` holds stable production code.
- Feature work occurs in `feature/<name>` branches.
- Submit pull requests to `main` for review.
- Keep commits focused and rebase frequently.
