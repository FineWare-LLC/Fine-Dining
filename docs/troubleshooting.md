# Troubleshooting

- **App fails to start**: ensure MongoDB and environment variables are configured.
- **No restaurants returned**: verify your Google Places API key or Overpass endpoint.
- **Build errors**: run `npm install` to install missing dependencies.

If issues persist, open an issue with logs and steps to reproduce.
